<nav class="navbar navbar-expand-md custom-navbar fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">intn</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <a class="nav-link " aria-current="page" href="/eventList">Lista Eventi</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link " href=" " tabindex="-1" aria-disabled="true"></a>
                </li>
            </ul>
            <form action="/search" method="POST" class="form-inline my-2 my-lg-0" role="search">
                <input class="form-control mr-sm-2" type="search" id="searchInput" aria-label="Search" name="searchTerm" placeholder="Cerca...">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Cerca</button>
            </form>
            <ul class="nav navbar-nav ml-auto">
                <% if (user && (user.role === 'company' || user.role === 'admin')) { %>
                    <a href="/events/create" class="btn btn-outline-success my-2 my-sm-0">+Crea</a>
                <% } else if (user && (user.role === 'user' || user.role === 'admin')) { %>
                    <a href="/events/create" class="btn btn-outline-success my-2 my-sm-0">+Crea</a>
                <% } %>
                <% if (!user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/signup"><span class="fas fa-user"></span> Sign Up</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/login"><span class="fas fa-sign-in-alt"></span> Login</a>
                    </li>
                <% } else { %>
                    <li>
                        <a class="nav-link" href="/profile">Il mio profilo</a>
                    </li>
                    <li>
                        <a class="nav-link" href="/logout"><span class="fas fa-sign-in-alt"></span>Logout</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>